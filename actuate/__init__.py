"""Actuate — an engineering framework for building closed-loop AI control
systems.

See docs/architecture/ for the frozen v1 architecture. Phase A (this
release) is the pure domain model in `actuate.domain`; the execution
engine, capability implementations, persistence, and UI land in later
phases per the migration plan.
"""

__version__ = "0.1.0"
